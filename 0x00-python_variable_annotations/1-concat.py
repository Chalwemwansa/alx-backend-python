#!/usr/bin/env python3
"""this python module contains a function that is used to concat
    two string together and has type annotation"""


def concat(str1: str, str2: str) -> str:
    """ the concat function that concatenates the two strings
        pased as arguments"""
    return (str1 + str2)
